using System;
using System.Collections;
using System.Collections.Generic;

using UnityEngine;

// Class to move a GameObject through a set of points. Meant to correspond to an Animation
public class AvatarMove : MonoBehaviour
{
    public List<Vector3> pointsToMoveThrough; // list of points to move the avatar through


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
